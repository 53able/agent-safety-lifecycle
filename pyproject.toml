[project]
name = "agent-safety-lifecycle"
version = "0.2.0"
description = "Agent Skills for maximizing bounded AI autonomy while minimizing human handoffs"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "53able" }]

[tool.unittest]
start-directory = "tests"
